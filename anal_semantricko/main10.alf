main
{
  int z, res;

  function int mayorQue5(int arg)
  {
    int auxArg, rv;
    auxArg = arg * 3;
    printf auxArg;
    rv = 1;
    if((auxArg < 5)){
      rv = 0;
    }
    return rv;
  }

  z = 2;
  res = mayorQue5(z);
  printf res;
}
