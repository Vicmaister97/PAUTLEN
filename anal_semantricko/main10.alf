main
{
  int z, res;

  function int mayorQue5(int arg)
  {
    int auxArg, rv;
    auxArg = arg;
    printf arg;
    rv = 1;
    if((auxArg < 5)){
      rv = 0;
    }
    else{
      rv=100;
    }
    return rv;
  }

  scanf z;
  printf z;
  res = mayorQue5(z);
  printf res;
}
