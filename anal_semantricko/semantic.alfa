// Incrementa en 1 el
// dato de entrada
// y muestra el resultado

main
{
	int x,y;
    int z;
    boolean bol;
  

 

    // Aritmetica
	x = 9-5;
    // Logica
    bol = (x < 7);
    // Vector


    if( (x == y) ){
        printf x;
        }
    else{
        while ( (y < 120) ){
        y = y + 1;
        }
    }
    
    printf true&&false;
    scanf x;
	printf y;

}
